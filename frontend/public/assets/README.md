# Brand Assets

Drop the three provided logo files here. Filenames must match exactly:

| File            | Used by                                                        | Notes              |
| --------------- | -------------------------------------------------------------- | ------------------ |
| `GS_Shield.png` | Favicon, apple-touch-icon, Header badge, BottomNav, PaywallGate | 512×512 app icon   |
| `GS_Name.png`   | Header wordmark, loading screens                               | 36px height        |
| `GS_Crest.png`  | Landing hero, Onboarding splash                               | 280px max width    |

Until these files exist, the `<img>` tags render their `alt` text. Everything
else in the app works without them.
